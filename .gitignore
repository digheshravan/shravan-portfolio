# Dependencies
node_modules

# Build output
dist
dist-ssr
.vite

# Local editor / tooling state
.claude/settings.local.json
# Codebase-memory index artifact. The graph is rebuilt on demand; the
# architectural reasoning lives in docs/adr, which is the part worth versioning.
.codebase-memory/
.vscode/*
!.vscode/extensions.json
.idea

# The served copy lives in public/Shravan-Dige-CV.pdf — this is the source file
# it was made from, and it carries a phone number, so it stays out of the repo.
Shravan_CV*.pdf

# Internship report. Contains a former employer's internal business strategy,
# named mentors, roll/SAP identifiers, and an unresolved security item in a live
# product. It is source material for writing the site — never a published asset.
*Final Report TIP*.pdf
*Internship*Report*.pdf

# OS noise
.DS_Store
Thumbs.db

# Env
*.local
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
